{% set port = (domain | split(':'))[1] %}
{% set host = domain | split(':') | first %}
server {
  listen {% block port %}{{ port }}{% endblock port %};
  proxy_pass {% block upstream %}{{ serviceName }}{% endblock upstream %};
  proxy_connect_timeout 10s;
}
