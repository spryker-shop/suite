SPRYKER_BROKER_ENGINE=rabbitmq
SPRYKER_BROKER_API_HOST={{ serviceName }}
SPRYKER_BROKER_API_PORT={{ serviceData['api']['port'] | default(15672) }}
SPRYKER_BROKER_API_USERNAME={{ serviceData['api']['username'] | default('spryker') }}
SPRYKER_BROKER_API_PASSWORD={{ serviceData['api']['password'] | default('secret') }}
SPRYKER_BROKER_HOST={{ serviceName }}
SPRYKER_BROKER_PORT={{ serviceData['port'] | default(5672) }}
SPRYKER_BROKER_USERNAME={{ serviceData['username'] | default('spryker') }}
SPRYKER_BROKER_PASSWORD={{ serviceData['password'] | default('secret') }}
SPRYKER_BROKER_CONNECTIONS={{ brokerConnections | raw }}
# STORE-SPECIFIC
#SPRYKER_BROKER_NAMESPACE=
